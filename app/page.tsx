/**
 * Renders a full-screen, centered container that displays the text "ghost AI".
 *
 * @returns The React element for a full-height flex container that centers its contents and shows "ghost AI".
 */
export default function Home() {
  return (
    <div className="flex h-screen items-center justify-center">
      <p>ghost AI</p>
      
    </div>
  );
}
